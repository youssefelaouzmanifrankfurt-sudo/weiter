<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neon Tasks</title>
    
    <script src="/socket.io/socket.io.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/aufgaben.css">

    <style>
        /* ZUSÄTZLICHES CSS FÜR TABS */
        .tabs { display: flex; gap: 10px; margin-bottom: 20px; border-bottom: 1px solid #222; padding-bottom: 10px; }
        .tab-btn { 
            background: transparent; border: 1px solid transparent; color: #666; 
            padding: 8px 20px; border-radius: 8px; cursor: pointer; font-weight: 600; font-size: 1rem; 
            transition: 0.2s;
        }
        .tab-btn:hover { color: #ccc; }
        .tab-btn.active { background: #1a1a1a; color: #10b981; border-color: #10b981; }
        
        .section-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; }
        .counter-badge { font-size: 0.8rem; background: #222; padding: 2px 8px; border-radius: 10px; margin-left: 5px; color: #888; }
        
        /* Archiv spezifisch */
        #grid-archive .task-card { opacity: 0.7; border-color: #333; }
        #grid-archive .task-card:hover { opacity: 1; border-color: #555; }
    </style>
</head>
<body>

    <%- include('partials/nav') %>

    <div class="container">
        
        <div class="input-panel">
            <h2>➕ Neue Aufgabe</h2>
            
            <div class="form-group">
                <label>Dein Name</label>
                <input type="text" id="user-name" placeholder="Wer bist du?" onchange="saveName()">
            </div>

            <div class="form-group">
                <label>Was muss gemacht werden?</label>
                <textarea id="task-text" rows="3" placeholder="Beschreibung..."></textarea>
            </div>

            <button class="btn-add" onclick="addTask()">Aufgabe erstellen</button>
        </div>

        <div class="task-list">
            
            <div class="tabs">
                <button class="tab-btn active" onclick="switchTab('open')" id="tab-open">
                    🔥 Offen <span id="count-open" class="counter-badge">0</span>
                </button>
                <button class="tab-btn" onclick="switchTab('archive')" id="tab-archive">
                    🗄️ Archiv <span id="count-archive" class="counter-badge">0</span>
                </button>
            </div>

            <div id="view-open">
                <div class="section-header">
                    <h3 style="margin:0; color:#ddd;">Aktuelle To-Dos</h3>
                </div>
                <div id="grid-open" class="task-grid"></div>
            </div>

            <div id="view-archive" style="display:none;">
                <div class="section-header">
                    <h3 style="margin:0; color:#888;">Erledigte Aufgaben</h3>
                    <button onclick="deleteCompleted()" style="background:rgba(239,68,68,0.1); border:1px solid #ef4444; color:#ef4444; padding:6px 15px; border-radius:8px; cursor:pointer; font-weight:bold; font-size:0.85rem; transition:0.2s;">
                        🗑 Alle unwiderruflich löschen
                    </button>
                </div>
                <div id="grid-archive" class="task-grid"></div>
            </div>

        </div>

    </div>

    <script src="/js/aufgaben.js"></script>
</body>
</html>